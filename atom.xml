<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>Huw Campbell</title>
    <link href="https://huwcampbell.com/atom.xml" rel="self" />
    <link href="https://huwcampbell.com" />
    <id>https://huwcampbell.com/atom.xml</id>
    <author>
        <name>Huw Campbell</name>
        <email>huw@huwcampbell.com</email>
    </author>
    <updated>2017-06-09T00:00:00Z</updated>
    <entry>
    <title>Announcing optparse-applicative 0.14.0</title>
    <link href="https://huwcampbell.com/posts/2017-06-09-announcing-optparse-0.14.html" />
    <id>https://huwcampbell.com/posts/2017-06-09-announcing-optparse-0.14.html</id>
    <published>2017-06-09T00:00:00Z</published>
    <updated>2017-06-09T00:00:00Z</updated>
    <summary type="html"><![CDATA[<div class="info">
    Posted on June  9, 2017
    
</div>

<p>I’m very pleased to announce the next major version of <a href="https://github.com/pcapriotti/optparse-applicative">optparse-applicative</a>. I was meaning to release this last month, but I had a baby last month instead, which has taken up quite a bit of my time. Still, it’s been a pleasure to put this release together, and I hope people enjoy the results. In this post, I’ll mention a few of the new features and fixes.</p>
<p><a href="https://hackage.haskell.org/package/optparse-applicative-0.14.0.0"><img src="https://img.shields.io/badge/hackage-v0.14.0.0-orange.svg" alt="Hackage page (downloads and API reference)" /></a></p>
<h2 id="bash-zsh-and-fish-completions">Bash, Zsh, and Fish Completions</h2>
<p>If you’re not yet using the completions scripts for optparse it’s well worth giving it a try now. I’ve fixed quite a lot of bugs, and added a few things which almost completely negate the need for writing custom shell scripts for completions for optparse programs at all.</p>
<p>The first few bugs related to how bash compgen actions were called, and these bugs got pretty ugly at times. For example, if one tried to complete a term which was partially quoted, such as</p>
<pre><code>&gt; example &#39;Documents/&lt;tab&gt;</code></pre>
<p>then a particularly awful error message regarding syntax errors would be emitted to stderr by the called bash process as it tried to find the close to the quotion.</p>
<p>The solution was to dequote any quoted or partially quoted string, then hard quote it again before passing it to the <code>compgen</code> builtin in bash. This also had the added benefit of fixing one of my personal annoyances for our completion system – premature expansion of <code>~</code> in a path completion: where</p>
<pre><code>~/Doc&lt;tab&gt;</code></pre>
<p>would become</p>
<pre><code>/home/huw/Documents</code></pre>
<p>This is now fixed, and the <code>~</code> does not get rewritten on the command line by a bash completion.</p>
<p>Beyond bash though, we now support much richer completions for zsh and fish shells. What’s particularly nice is that these completions also now can include the help descriptions for the items being completed.</p>
<p>Here is an example screenshot when using fish shell using only the uncustomised optparse generated completion script</p>
<p><img src="/images/fish-completions.png" alt="Fish Completion" class="u-full-width" style="border-radius: 4px; border: 1px solid #E1E1E1;"/></p>
<p>A similar level of detail is available with zsh completions as well.</p>
<h2 id="error-messages">Error Messages</h2>
<p>One of my focuses for this release was to ensure that optparse gives a great error message for <em>every</em> type of failure we emit (this has actually been a focus of mine for some time); and there was one which was oddly handled by default.</p>
<p>Since 0.5 <code>option</code> has had a builder, <code>noArgError</code> which one can use to specify the error message shown to a user if they do not provide an argument for the respective option. We have used this for the provided <code>helper</code> parser for instance such that it always shows the help text. The default for this was a conspicuously unhelpful though, being <code>ErrorMsg &quot;&quot;</code>. Now instead of not receiving an error message, one should see</p>
<pre><code>The option `--bash-completion-script` expects an argument.</code></pre>
<p>for example. This did require a change to the internal types to store the name of the option, but I managed to keep the builder api the same, so one can still use the old builder to change the error message if they desire.</p>
<p>Combined with other error message improvements mentioned in <a href="/posts/2017-02-28-maintaining-optparse-applicative.html">my previous post</a> I don’t think one will ever receive an unhelpful or absent error message from an <code>optparse-applicative</code> parser going forwards, which I think is a great improvement.</p>
<h2 id="suggestions">Suggestions</h2>
<p>When an option, flag, or subcommand can’t be parsed, or no match for it could be found, we now can emit some suggestions of terms which the user may have meant. This is based off the edit distance between the entered argument and the possible items the parser knows about. Details regarding the implementation, are available in <a href="/posts/2017-02-28-maintaining-optparse-applicative.html">my last post</a></p>
<p>The end result looks a lot like git’s command completion, and is quite nice I think</p>
<pre><code>Invalid argument `installl&#39;

Did you mean this?
    install</code></pre>
<pre><code>Invalid option `-x&#39;

Did you mean one of these?
    -v
    -h</code></pre>
<h2 id="no-interperse-and-forward-options">No Interperse and Forward Options</h2>
<p>Optparse has had a no intersperse option builder for a good while (since 0.7), but its description and behaviour didn’t actually match. When a parser was called with <code>noInterperse</code> applied, it would behave the same as if the user had typed <code>--</code> at the start of the command: that is, no options would be parsed. In other parsing libraries with no intersperse options on (and old unix command line guidelines), one has to place all options before any arguments. Once a single positional argument is parsed, all remaining options are treated as positional as well. We now behave in the same way.</p>
<p>Fixing this issue inspired me to also alleviate a pain point which my colleagues and I had been having regarding mixing arguments and wrapping commands. At Ambiata, we created a build tool called <a href="https://github.com/ambiata/mafia">mafia</a> which acts as an opinionated wrapper around <code>cabal</code>, providing a <code>nix</code> like package cache, safe sandboxing, and submodule handling amongst other things.</p>
<p>When using it though, to pass an option to <code>cabal</code> underneath, the user would need to first use <code>--</code> to tell optparse to treat all remaining options as arguments, and the argument list would be sent to <code>cabal</code>. Now, with the <code>forwardOptions</code> builder attached when creating the <code>parserInfo</code> structures, any unrecognised options to <code>mafia</code> can be passed through as is. I believe this will actually help in quite a few places where people are using mixed environments (such as a <a href="https://github.com/pcapriotti/optparse-applicative/pull/259">C</a> command line parser).</p>
<h2 id="polymorphic-string">Polymorphic String</h2>
<p>Text and Bytestring are pervasive in the Haskell ecosystem, so it was annoying that <code>optparse-applicative</code> didn’t support them trivially out of the box.</p>
<p>The builders we have provided for some time are</p>
<pre><code>str :: ReadM String
auto :: Read a =&gt; ReadM a</code></pre>
<p>which I have found quite funny, in that <code>auto</code> is good for simple types apart from string like ones (which require quoting when used with <code>read</code>) while <code>str</code> requires an <code>fmap pack</code> to make it work with <code>Text</code>. From 0.14 onwards these will be</p>
<pre><code>str :: IsString a =&gt; ReadM a
auto :: Read a =&gt; ReadM a</code></pre>
<p>which means that the <code>str</code> reader will be able to deal with <code>String</code>, <code>Text</code>, and <code>ByteString</code>, as well as their lazy equivalents without requiring the user to jump through any hoops. Other builders and helpers such as <code>strOption</code> and <code>strArgument</code> have also been updated to be polymorphic over <code>IsString</code> in the same manner. This is a mildly breaking change, but I think it’s worthwhile.</p>
<p>If one has been using <code>str</code> for the purposes of type inference (as we were in the <code>optparse-applicative</code> test suite) then type signatures may now be required, or the use of <code>readerAsk</code>, which is still fixed to a single type. As there’s not a lot of purpose in requesting a value and not using it, I don’t think many users will see any update issues from this change at all.</p>
<h2 id="explicit-module-exports">Explicit Module Exports</h2>
<p>The final big thing I have done before release is to remove the module reëxports from the <code>Options.Applicative</code> module and explicitly export a public API.</p>
<p>This should make things more robust going forwards, and let people know exactly what is a supported and useful part of the API, instead of having them have to infer if something is actually internal to the library.</p>
<p>In doing this I have actually removed a few function exports which were internal or deprecated, and added the <code>ParserHelp</code> type (which is useful) to the main module exports.</p>
<p>Again, this is a minorly breaking change, it is however quite unlikely that it will cause issues for most users, and indeed, it should make it easier to maintain a rock solid API going forwards.</p>
<h2 id="thanks">Thanks</h2>
<p>Thanks to Paolo Capriotti for trusting me to maintain his library, and to those who have submitted patches and bug reports and given advice. Herbert Valerio Riedel was especially helpful with keeping things compatible across past and (with any luck) future versions of GHC.</p>
]]></summary>
</entry>
<entry>
    <title>Oven Poached Chicken Drumsticks</title>
    <link href="https://huwcampbell.com/posts/2017-05-20-coconut-poaced-chicken.html" />
    <id>https://huwcampbell.com/posts/2017-05-20-coconut-poaced-chicken.html</id>
    <published>2017-05-20T00:00:00Z</published>
    <updated>2017-05-20T00:00:00Z</updated>
    <summary type="html"><![CDATA[Recipe for delicious coconut poached chicken]]></summary>
</entry>
<entry>
    <title>8 Queens Problem</title>
    <link href="https://huwcampbell.com/posts/2017-05-11-8-queens-problem.html" />
    <id>https://huwcampbell.com/posts/2017-05-11-8-queens-problem.html</id>
    <published>2017-05-11T00:00:00Z</published>
    <updated>2017-05-11T00:00:00Z</updated>
    <summary type="html"><![CDATA[<div class="info">
    Posted on May 11, 2017
    
</div>

<p>I heard of this problem recently, based on Aphyr’s eccentrically written <a href="https://aphyr.com/posts/342-typing-the-technical-interview">typing the technical interview</a> blog post. It’s an interesting read, but sadly, I am not a witch, so I found the functional dependency style used quite difficult to understand. I therefore thought I would spend a few minutes bashing out a simple solution at the term level, then attempt to port it to the type level using more modern Haskell techniques with the <code>singletons</code> library and type families.</p>
<p>The question, which is apparently common in interviews, is to discover ways of placing 8 queens on a chessboard without any of them being able to attack one another.</p>
<p>Here’s my take at the term level version: my solution is a monadic fold using list’s monad instance, where the accumulator is keeping track of the Queens’ positions in each row we have placed already.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">import</span> <span class="dt">Control.Monad</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"><span class="ot">solution ::</span> [[<span class="dt">Int</span>]]</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">solution <span class="fu">=</span> foldM place [] [<span class="dv">1</span><span class="fu">..</span><span class="dv">8</span>]</a>
<a class="sourceLine" id="cb1-5" data-line-number="5"></a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="ot">place ::</span> [<span class="dt">Int</span>] <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> [[<span class="dt">Int</span>]]</a>
<a class="sourceLine" id="cb1-7" data-line-number="7">place current _ <span class="fu">=</span></a>
<a class="sourceLine" id="cb1-8" data-line-number="8">  (<span class="fu">:</span> current) <span class="fu">&lt;$&gt;</span> filter (safe current) [<span class="dv">1</span><span class="fu">..</span><span class="dv">8</span>]</a>
<a class="sourceLine" id="cb1-9" data-line-number="9"></a>
<a class="sourceLine" id="cb1-10" data-line-number="10"><span class="ot">safe ::</span> [<span class="dt">Int</span>] <span class="ot">-&gt;</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Bool</span></a>
<a class="sourceLine" id="cb1-11" data-line-number="11">safe xs x <span class="fu">=</span></a>
<a class="sourceLine" id="cb1-12" data-line-number="12">  and</a>
<a class="sourceLine" id="cb1-13" data-line-number="13">    [ all (<span class="fu">/=</span> x) xs</a>
<a class="sourceLine" id="cb1-14" data-line-number="14">    , all (<span class="fu">/=</span> x) (zipWith (<span class="fu">+</span>) xs [<span class="dv">1</span><span class="fu">..</span>])</a>
<a class="sourceLine" id="cb1-15" data-line-number="15">    , all (<span class="fu">/=</span> x) (zipWith (<span class="fu">-</span>) xs [<span class="dv">1</span><span class="fu">..</span>])</a>
<a class="sourceLine" id="cb1-16" data-line-number="16">    ]</a></code></pre></div>
<p>It’s a simple solution. For the type level, I’ll be doing it quite differently to Aphyr.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="ot">{-# LANGUAGE TypeOperators, DataKinds, PolyKinds, TypeFamilies, KindSignatures,</span></a>
<a class="sourceLine" id="cb2-2" data-line-number="2"><span class="ot">             TemplateHaskell, GADTs, UndecidableInstances, RankNTypes,</span></a>
<a class="sourceLine" id="cb2-3" data-line-number="3"><span class="ot">             ScopedTypeVariables, FlexibleContexts, TypeInType #-}</span></a>
<a class="sourceLine" id="cb2-4" data-line-number="4"></a>
<a class="sourceLine" id="cb2-5" data-line-number="5"><span class="kw">module</span> <span class="dt">Main</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-6" data-line-number="6"></a>
<a class="sourceLine" id="cb2-7" data-line-number="7"><span class="kw">import</span> <span class="dt">Data.Singletons</span></a>
<a class="sourceLine" id="cb2-8" data-line-number="8"><span class="kw">import</span> <span class="dt">Data.Singletons.Prelude</span></a>
<a class="sourceLine" id="cb2-9" data-line-number="9"><span class="kw">import</span> <span class="dt">Data.Singletons.Prelude.List</span></a>
<a class="sourceLine" id="cb2-10" data-line-number="10"><span class="kw">import</span> <span class="dt">Data.Singletons.TypeLits</span></a></code></pre></div>
<p>With that out of the way I can define a type level <code>safe</code> function which returns all the safe locations which a queen can go in a row, given the positions of queens in the previous rows.</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="kw">type</span> family <span class="dt">Safe</span> (<span class="ot">b ::</span> [<span class="dt">Nat</span>]) (<span class="ot">a ::</span> <span class="dt">Nat</span>)<span class="ot"> ::</span> <span class="dt">Bool</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-2" data-line-number="2">  <span class="dt">Safe</span> xs x <span class="fu">=</span></a>
<a class="sourceLine" id="cb3-3" data-line-number="3">    <span class="dt">And</span></a>
<a class="sourceLine" id="cb3-4" data-line-number="4">      &#39;[ <span class="dt">All</span> ((<span class="fu">:/=$$</span>) x) xs</a>
<a class="sourceLine" id="cb3-5" data-line-number="5">       , <span class="dt">All</span> ((<span class="fu">:/=$$</span>) x)</a>
<a class="sourceLine" id="cb3-6" data-line-number="6">         ( <span class="dt">ZipWith</span> (<span class="fu">:+$</span>) xs                   (<span class="dt">EnumFromTo</span> <span class="dv">1</span> <span class="dv">8</span>) )</a>
<a class="sourceLine" id="cb3-7" data-line-number="7">       , <span class="dt">All</span> ((<span class="fu">:/=$$</span>) ( <span class="dv">10</span> <span class="fu">:+</span> x ))</a>
<a class="sourceLine" id="cb3-8" data-line-number="8">         ( <span class="dt">ZipWith</span> (<span class="fu">:-$</span>) (<span class="dt">Map</span> ((<span class="fu">:+$$</span>) <span class="dv">10</span>) xs) (<span class="dt">EnumFromTo</span> <span class="dv">1</span> <span class="dv">8</span>) ) ]</a></code></pre></div>
<p>I was tripped up a few times writing this function. The first thing was just trying to find the right version of the equality functions to call. The <code>$$</code> ending specifies the right number of partial applications to apply, but it’s a real pain to discover due to the template haskell generated haddocks of the <code>singletons</code> library being terrible. The next thing was the natural number subtraction. My type families were getting stuck without the <code>+10</code> when a negative number would have been produced, but it was quite hard to see what the problem really was.</p>
<p>Below I define my partial application for <code>Safe</code>. Unfortunately, just like Aphyr I still need to do this as Haskell type application can’t be partially applied. But using data kinds and type in type makes this a lot safer here, with a proper type signature keeping everything in place.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="kw">data</span> <span class="dt">Safe1</span><span class="ot"> ::</span> [<span class="dt">Nat</span>] <span class="ot">-&gt;</span> (<span class="dt">Nat</span> <span class="fu">~&gt;</span> <span class="dt">Bool</span>)</a>
<a class="sourceLine" id="cb4-2" data-line-number="2"><span class="kw">type</span> <span class="kw">instance</span> <span class="dt">Apply</span> (<span class="dt">Safe1</span> xs) x <span class="fu">=</span> <span class="dt">Safe</span> xs x</a></code></pre></div>
<p>The <code>(~&gt;)</code> and the <code>Apply</code> type family come from the <code>singletons</code> library, and represent type level functions, and their application respectively. Next, we filter a candidate set of positions using the safe function, with the help of some additional type families from <code>Data.Singletons.Prelude.List</code>.</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="kw">type</span> family <span class="dt">Place</span> (<span class="ot">a ::</span> [<span class="dt">Nat</span>]) (<span class="ot">b ::</span> k)<span class="ot"> ::</span> [[<span class="dt">Nat</span>]] <span class="kw">where</span></a>
<a class="sourceLine" id="cb5-2" data-line-number="2">  <span class="dt">Place</span> xs ignore <span class="fu">=</span></a>
<a class="sourceLine" id="cb5-3" data-line-number="3">    <span class="dt">Map</span> (<span class="dt">FlipCons1</span> xs) (<span class="dt">Filter</span> (<span class="dt">Safe1</span> xs) (<span class="dt">EnumFromTo</span> <span class="dv">1</span> <span class="dv">8</span>))</a>
<a class="sourceLine" id="cb5-4" data-line-number="4"></a>
<a class="sourceLine" id="cb5-5" data-line-number="5"><span class="kw">data</span> <span class="dt">FlipCons1</span><span class="ot"> ::</span> [<span class="dt">Nat</span>] <span class="ot">-&gt;</span> (<span class="dt">Nat</span> <span class="fu">~&gt;</span> [<span class="dt">Nat</span>])</a>
<a class="sourceLine" id="cb5-6" data-line-number="6"><span class="kw">type</span> <span class="kw">instance</span> <span class="dt">Apply</span> (<span class="dt">FlipCons1</span> xs) x <span class="fu">=</span> x &#39;<span class="fu">:</span> xs</a>
<a class="sourceLine" id="cb5-7" data-line-number="7"></a>
<a class="sourceLine" id="cb5-8" data-line-number="8"><span class="kw">data</span> <span class="dt">Place1</span><span class="ot"> ::</span> [<span class="dt">Nat</span>] <span class="ot">-&gt;</span> (ignore <span class="fu">~&gt;</span> [[<span class="dt">Nat</span>]])</a>
<a class="sourceLine" id="cb5-9" data-line-number="9"><span class="kw">type</span> <span class="kw">instance</span> <span class="dt">Apply</span> (<span class="dt">Place1</span> xs) b <span class="fu">=</span> <span class="dt">Place</span> xs b</a>
<a class="sourceLine" id="cb5-10" data-line-number="10"></a>
<a class="sourceLine" id="cb5-11" data-line-number="11"><span class="kw">data</span> <span class="dt">Place2</span><span class="ot"> ::</span> ([<span class="dt">Nat</span>] <span class="fu">~&gt;</span> ignore <span class="fu">~&gt;</span> [[<span class="dt">Nat</span>]])</a>
<a class="sourceLine" id="cb5-12" data-line-number="12"><span class="kw">type</span> <span class="kw">instance</span> <span class="dt">Apply</span> (<span class="dt">Place2</span>) xs <span class="fu">=</span> <span class="dt">Place1</span> xs</a></code></pre></div>
<p>To write the FoldM we need to perform type application using the <code>Apply</code> instances. This is actually pretty easy, and there is even a <code>(@@)</code> operator to help us here. Again, we also need to define a function for the partially applied recursive term.</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb6-1" data-line-number="1"><span class="kw">type</span> family <span class="dt">FoldM</span> (<span class="ot"> f ::</span> b <span class="fu">~&gt;</span> a <span class="fu">~&gt;</span> [b] ) (<span class="ot"> acc ::</span> b ) (<span class="ot"> over ::</span> [a] )<span class="ot"> ::</span> [b] <span class="kw">where</span></a>
<a class="sourceLine" id="cb6-2" data-line-number="2">  <span class="dt">FoldM</span> f acc &#39;[] <span class="fu">=</span> &#39;[ acc ]</a>
<a class="sourceLine" id="cb6-3" data-line-number="3">  <span class="dt">FoldM</span> f acc ( x &#39;<span class="fu">:</span> xs) <span class="fu">=</span></a>
<a class="sourceLine" id="cb6-4" data-line-number="4">    <span class="dt">ConcatMap</span> (<span class="dt">FoldM1</span> f xs) (f <span class="fu">@@</span> acc <span class="fu">@@</span> x )</a>
<a class="sourceLine" id="cb6-5" data-line-number="5"></a>
<a class="sourceLine" id="cb6-6" data-line-number="6"><span class="kw">data</span> <span class="dt">FoldM1</span><span class="ot"> ::</span> ( b <span class="fu">~&gt;</span> a <span class="fu">~&gt;</span> [b] ) <span class="ot">-&gt;</span> [a] <span class="ot">-&gt;</span> ( b <span class="fu">~&gt;</span> [b] )</a>
<a class="sourceLine" id="cb6-7" data-line-number="7"><span class="kw">type</span> <span class="kw">instance</span> <span class="dt">Apply</span> (<span class="dt">FoldM1</span> f xs ) acc <span class="fu">=</span> <span class="dt">FoldM</span> f acc xs</a></code></pre></div>
<p>Next up is the solution. It’s written almost identically to the term level code above, with a fold over placements.</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb7-1" data-line-number="1"><span class="kw">type</span> family <span class="dt">Solutions</span><span class="ot"> ::</span> [[<span class="dt">Nat</span>]] <span class="kw">where</span></a>
<a class="sourceLine" id="cb7-2" data-line-number="2">  <span class="dt">Solutions</span> <span class="fu">=</span> <span class="dt">FoldM</span> <span class="dt">Place2</span> &#39;[] (<span class="dt">EnumFromTo</span> <span class="dv">1</span> <span class="dv">8</span>)</a></code></pre></div>
<p>It appears to work too</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb8-1" data-line-number="1"><span class="fu">*</span><span class="dt">Main</span><span class="fu">&gt;</span> <span class="fu">:</span>t <span class="dt">Proxy</span><span class="ot"> ::</span> <span class="dt">Proxy</span> <span class="dt">Solutions</span></a>
<a class="sourceLine" id="cb8-2" data-line-number="2"><span class="dt">Proxy</span><span class="ot"> ::</span> <span class="dt">Proxy</span> <span class="dt">Solutions</span></a>
<a class="sourceLine" id="cb8-3" data-line-number="3"><span class="ot">  ::</span> <span class="dt">Proxy</span></a>
<a class="sourceLine" id="cb8-4" data-line-number="4">       (&#39;[<span class="dv">4</span>, <span class="dv">2</span>, <span class="dv">7</span>, <span class="dv">3</span>, <span class="dv">6</span>, <span class="dv">8</span>, <span class="dv">5</span>, <span class="dv">1</span>]</a>
<a class="sourceLine" id="cb8-5" data-line-number="5">        <span class="fu">:$$$</span> <span class="ch">&#39;[&#39;</span>[<span class="dv">5</span>, <span class="dv">2</span>, <span class="dv">4</span>, <span class="dv">7</span>, <span class="dv">3</span>, <span class="dv">8</span>, <span class="dv">6</span>, <span class="dv">1</span>], &#39;[<span class="dv">3</span>, <span class="dv">5</span>, <span class="dv">2</span>, <span class="dv">8</span>, <span class="dv">6</span>, <span class="dv">4</span>, <span class="dv">7</span>, <span class="dv">1</span>],</a>
<a class="sourceLine" id="cb8-6" data-line-number="6">        <span class="fu">...</span></a></code></pre></div>
<h2 id="conclusions">Conclusions</h2>
<p>Modern Haskell’s type level code is actually pretty succinct, and even has a good degree of type safety. Using less code, we are pretty easily able to get all the solutions (instead of one in the cited blog post), and my type errors when writing it actually existed, which was a plus.</p>
<h2 id="bonus---as-template-haskell">Bonus - as Template Haskell</h2>
<p>Interestingly, a lot of what was written above can be generated for us by the <code>singletons</code> library (indeed, this is how the <code>singletons</code> library is written).</p>
<p>For example, the <code>FoldM</code> type family above can be completely replaced with</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb9-1" data-line-number="1"><span class="kw">import</span> <span class="dt">Data.Singletons.TH</span></a>
<a class="sourceLine" id="cb9-2" data-line-number="2"><span class="fu">$</span>(singletonsOnly [d|</a>
<a class="sourceLine" id="cb9-3" data-line-number="3">  foldM :: (b -&gt; a -&gt; [b]) -&gt; b -&gt; [a] -&gt; [b]</a>
<a class="sourceLine" id="cb9-4" data-line-number="4">  foldM _ acc [] = [acc]</a>
<a class="sourceLine" id="cb9-5" data-line-number="5">  foldM f acc (x : xs) =</a>
<a class="sourceLine" id="cb9-6" data-line-number="6">    concatMap (\acc&#39; -&gt; foldM f acc&#39; xs) (f acc x)</a>
<a class="sourceLine" id="cb9-7" data-line-number="7">  |])</a></code></pre></div>
<p>and the library will even generate for us the partially applied terms.</p>
<p>One can actually get very close to the result with just the code from my term level calculation in a <code>singletonsOnly</code> splice. It is however a bit finicky, and often complains about functions not existing when there is a type family for them, so it’s a bit all or nothing. Plus, one loses a lot of readability in their error messages and the haddocks will be phenomenally bad.</p>
]]></summary>
</entry>
<entry>
    <title>Maintaining Optparse-Applicative</title>
    <link href="https://huwcampbell.com/posts/2017-02-28-maintaining-optparse-applicative.html" />
    <id>https://huwcampbell.com/posts/2017-02-28-maintaining-optparse-applicative.html</id>
    <published>2017-02-28T00:00:00Z</published>
    <updated>2017-02-28T00:00:00Z</updated>
    <summary type="html"><![CDATA[Recent additions to optparse-applicative]]></summary>
</entry>
<entry>
    <title>Grenade</title>
    <link href="https://huwcampbell.com/posts/2017-02-17-introduction-to-grenade.html" />
    <id>https://huwcampbell.com/posts/2017-02-17-introduction-to-grenade.html</id>
    <published>2017-02-17T00:00:00Z</published>
    <updated>2017-02-17T00:00:00Z</updated>
    <summary type="html"><![CDATA[An introduction to Grenade, deep learning in Haskell]]></summary>
</entry>

</feed>
